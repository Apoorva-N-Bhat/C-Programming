/*‘\a’ is an ASCII BEL character it is a terminal feature it produces a “beep” sound or a notification sound simultaneously displaying a terminal or command prompt(windows) */
#include <stdio.h>
main()
{
	printf("Hello World\a");  /* gives beep sound*/     
}